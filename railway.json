{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python3.11 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 300",
    "restartPolicyType": "ON_FAILURE"
  }
}
